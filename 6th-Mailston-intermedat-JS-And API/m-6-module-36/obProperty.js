const  bottle = {color:"yellow", hold:'water', price:50
};

// get only properties name
const keys = Object.keys(bottle);
//console.log(keys);

// get only properties value
const value = Object.values(bottle);
//console.log(value);

const pairs = Object.entries(bottle);
console.log(pairs);

